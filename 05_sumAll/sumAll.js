const sumAll = function (num1, num2) {
	if (
		typeof num1 != "number" ||
		typeof num2 != "number" ||
		num1 < 0 ||
		num2 < 0
	) {
		return "ERROR";
	} else {
		let output = 0;
		let start = 0;
		let end = 0;

// Do not edit below this line
module.exports = sumAll;
